import { AppLayout } from '../../components/layout/AppLayout';
import View from '../../views/RecruiterInbox';

export const metadata = {
  title: 'RecruiterInbox',
};

export default function Page() {
  return <AppLayout><View /></AppLayout>;
}
